/** 4pt grid. Screen horizontal padding in the reference is ~14pt on a 300pt-wide frame. */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  screen: 14,
  headerTop: 6,
  cardPadding: 14,
  tabBarHeight: 64,
} as const;
